apiVersion: v1
kind: Service
metadata:
  name: publicCloudLB-medium
  annotations:
    loadbalancer.ovhcloud.com/flavor: "medium"
  labels:
    app: test-publicCloudLB
spec:
  ports:
  - name: client
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: nginx
  type: LoadBalancer
