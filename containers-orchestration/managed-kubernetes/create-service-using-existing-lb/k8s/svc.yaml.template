apiVersion: v1
kind: Service
metadata:
  name: hello-world
  annotations:
    loadbalancer.openstack.org/load-balancer-id: $LB_ID
  labels:
    app: hello-world
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: hello-world